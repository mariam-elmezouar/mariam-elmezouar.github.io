function val =  BinComp2Dec(valbin)
    %this function takes as input a two's complement 8-bit signed binary (i.e. a binary that can be possibly positive or negative)
    %stored in the character array valbin
    %and returns the equivalent decimal in val

    % your code here
    val = 0;
end