import gogographics


def errors(drawing):
    e = drawing.new_ellipse()
    e.fill((255, 255, 255))
    e.fill((0, 0, 0))
    e.fill((250, 220, 220))
    # e.fill((255, 255, 256))
    # e.fill((256, 255, 255))
    # e.fill((255, 255, 256))
    # e.fill((0.0, 0.0, -0.0001))
    # e.fill((-1, 0, 0))
    # e.fill((0, -1, 0))
    e.stroke((100, 100, 250))
    # e.stroke((-1, 0, 0))
    e.visible(False)
    e.visible(True)
    # e.visible(7)
    e.move(200, -100)
    # e.move('a', 100)
    # e.move(-100, (3, 4))
    e.width(200)
    # e.width(-1)
    e.height(300)
    # e.height(-1)
    e.stroke_width(2.5)
    # e.stroke_width(-0.5)
    ln = drawing.new_line()
    ln.stroke((10, 190, 10))
    ln.length(300.0)
    # ln.length(-0.1)
    ln.stroke_width(4.2)
    # ln.stroke_width(-0.1)
    ln.start((-10, 200))
    # ln.start(50)
    ln.end((810, 600))
    # ln.end((5, 6, 7))
    t = drawing.new_text()
    t.string('anything is a string')
    t.height(50)
    # t.height('50')
    t.fill((180, 180, 10))
    t.fill(12)
gogographics.go(errors)
